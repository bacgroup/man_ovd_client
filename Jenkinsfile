node{
    stage('Buid')
    {
        echo 'Build'
        sh 'ls -lh'
        sh 'pwd'
        sh 'sudo npm install -g electron-packager'
        sh 'sudo npm install -g electron-installer-debian'
    }
    stage('Test')
    {
        echo 'Test'
    }
    stage('Deploy')
    {
        echo 'Deploy'
    }
}
